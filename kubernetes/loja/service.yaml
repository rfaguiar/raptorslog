apiVersion: v1
kind: Service
metadata:
  name: loja
  namespace: raptorslog
  labels:
    app: loja
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: 8090
  selector:
    app: loja