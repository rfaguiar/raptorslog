apiVersion: v1
kind: Service
metadata:
  labels:
    app: loja
  name: loja-svc
  namespace: raptorslog
spec:
  type: ClusterIP
  ports:
    - name: app-port
      protocol: TCP
      port: 80
      targetPort: 8090
  selector:
    app: loja